name: 
  First Workflow

on: 
  workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-22.04
    steps:
      - name: Print greeting
        run: echo -e "Hello, World"
      - name: Print goodbye
        run: echo -e "Done - Bye"


